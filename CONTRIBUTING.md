# CONTRIBUTING.md — 协作方式

主干开发。规则的**强制部分**在 [`CLAUDE.md`](CLAUDE.md) 与
[`AGENTS.md`](AGENTS.md)，本文只讲流程与取舍。

## 一、分支结构：一根主线 + 两类例外

```
main                 ← 所有人直接提这里
hotfix/*             ← main 红灯，修它
surgery/*            ← 内核契约级大改，中途状态会让 main 不可用
```

没有第三类。有价值的历史分支打 `archive/*` tag 留存，不作为分支存在。

**为什么不用 PR**：这个仓库当前是单人 + Agent 的节奏，PR 只是给自己发通知。
等有第二个人类常驻贡献者时再改，那时把这一节重写。

## 二、提交

见 `AGENTS.md` §1。钩子会拦，不用记全，写错了它会告诉你哪里错。

## 三、CI

只有一个 workflow：`.github/workflows/checks.yml`，push / PR / 手动都触发。
两个 job：

| job | 跑什么 |
|---|---|
| `framework` | `npm ci` → `npm run typecheck` → `npm test` |
| `contracts` | 守卫自测（坏样本必须被拦下）→ 契约守卫 |

**本仓库不产出对外产物，不做自动发版。** 包的发布（如果将来要发 npm）
一律手动触发，不挂在 push 上。

## 四、红灯协议（main 挂了怎么办）

1. **红灯优先于一切。** 别在红灯上继续堆功能。
2. `pre-push` 会拦：红灯期间推 main 的提交必须全是修复类
   （标题以 `fix(` / `fix:` / `revert` 开头）。
3. 修不动就 revert。**revert 不丢人，红灯挂一天才丢人。**
4. 需要多轮调试的开 `hotfix/*`，修好合回 main 并删分支。

红灯状态由钩子联网问 GitHub API（`checks.yml` 在 main 上最近一次跑完的结论）。
查不到一律放行并提示——护栏坏了让路，不锁仓库。

## 五、改了守卫本身，必须跑自测

```bash
python3 tools/test-check-sources.py
```

每加一条规则，就在自测里加一个会触发它的坏样本。
**一个拦不下任何东西的检查比没有检查更糟**——它会让人以为有人在看着。

同理，`tools/githooks/` 改动后手工验一遍：故意写一条不合规的提交信息，
确认它真的被拦。

## 六、最低限度沟通机制

开工时往 [`ACTIVE.md`](ACTIVE.md) 加一行，完工删行。10 秒成本，消灭最严重的对撞。

## 七、上游仓库的改造

本仓库要改上游查看器时（把它们包成插件），遵守
[`docs/VIEWER-REFACTOR.md`](docs/VIEWER-REFACTOR.md) 的两条铁律：

1. **上游仓库改造后必须仍能独立运行**——插件入口是新增的，不是替换；
2. **资源改造先于接口改造**——反过来做会在装上插件时把几个 G 塞进部署产物，
   而撞 Pages 限额时不报错。

上游各仓库有自己的提交规范（兄弟仓库客户端那套最严），
去那边提交就按那边的规矩来，不要把本仓库的约定带过去。

## 八、🔴 两条发布禁令（禁止解除）

见 [`AGENTS.md`](AGENTS.md) §4。`pre-push` 硬拦，改动要经维护者批准，
并同步修改上游仓库自己的策略文件。
