{
  "name": "@aio/framework",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm run test && node --test integrations/halo-aioviewer/test/story-router-contract.test.mjs && python3 -X utf8 tools/check-sources.py && python3 -X utf8 tools/check-assets.py && python3 -X utf8 tools/test-check-sources.py && python3 -X utf8 tools/test-check-assets.py && python3 -X utf8 tools/test-build-manifest.py && python3 -X utf8 tools/test-build-story-routes.py && python3 -X utf8 tools/test-build-halo-aioviewer.py && npm run story-router:build",
    "story-router:build": "npm run -w @aio/story-router build && npm run -w @aio/story-router-edge build",
    "edgeone:build": "npm run story-router:build",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.2",
    "vitepress": "^2.0.0-alpha.19",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  }
}
